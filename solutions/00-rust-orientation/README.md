# Lesson 00 solution — the supplied answer

This package contains the complete answer, **not your exercise implementation**. The [solution discussion](../../host/lessons/00-rust-orientation/SOLUTION.md) explains the comparison and boundary cases.

From this directory:

```bash
make test
```

Equivalent:

```bash
cargo test --locked --offline
```

All six supplied acceptance tests should pass. A passing result here does not verify your work in `exercises/00-rust-orientation/`.

Return to the [lesson](../../host/lessons/00-rust-orientation/README.md).
