[package]
name = "lesson-00-exercise"
version = "0.1.0"
edition = "2024"
rust-version = "1.90"
publish = false
license = "MIT"

# The exercise and solution expose the same name to their acceptance tests.
[lib]
name = "threshold_rule"
